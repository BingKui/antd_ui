import 'package:flutter/material.dart';

class AntdFooter extends StatelessWidget {
  const AntdFooter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
