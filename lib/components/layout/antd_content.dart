import 'package:flutter/material.dart';

class AntdContent extends StatelessWidget {
  final Widget child;
  const AntdContent({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
