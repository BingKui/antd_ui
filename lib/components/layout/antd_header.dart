import 'package:flutter/material.dart';

class AntdHeader extends StatelessWidget {
  const AntdHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
